import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFfef7f8); // soft pink background
  static const primary = Color(0xFFb738d5);     // vibrant purple (app bar & accents)
  static const secondary = Color(0xFFcc32bf);   // strong pink/purple secondary
  static const pink = Color(0xFFAC54EF);        // light pink shimmer
  static const purple = Color(0xFFE14AA6);      // lavender shimmer
  static const darkText = Color(0xFFFFFFFF);    // titles and body text
  static const lightText = Color(0xFF888888);   // subtitles and labels
  static const white = Colors.white;
  static const border = Color(0xFFf4dada);      // soft border tone
}
